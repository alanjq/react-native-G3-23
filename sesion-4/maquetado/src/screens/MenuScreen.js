import { Button, View } from 'react-native'

// URL: /
export default function MenuScreen() {
    const handleButtonPress = () => {
    }

    return <View>
        <Button title='Busca tu vuelo' onPress={handleButtonPress} />
    </View>
}
